<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nar_res"
    app:startDestination="@id/checkListFragment2">
    <fragment
        android:id="@+id/checkListFragment2"
        android:name="com.github.ambulance10.CheckListFragment"
        android:label="CheckListFragment"
        tools:layout="@layout/fragment_checklist">
        <action
            android:id="@+id/action_checkListFragment_to_editValueDialog"
            app:destination="@id/editValueDialog" />
    </fragment>
    <dialog
        android:id="@+id/editValueDialog"
        android:name="com.github.ambulance10.EditValueDialog"
        android:label="EditValueDialog"
        tools:layout="@layout/dialog_editvalue">
        <argument
            android:name="editValueID"
            app:argType="integer" />
    </dialog>
</navigation>